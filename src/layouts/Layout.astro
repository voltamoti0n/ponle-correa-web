---
// src/layouts/Layout.astro

// ---- ¡ESTA ES LA CORRECCIÓN! ----
// En lugar de usar una etiqueta <link> en el HTML,
// importamos el CSS directamente aquí.
// Astro se encargará de procesarlo y añadir el enlace correcto
// al HTML final durante la construcción (build).
import '../styles/global.css';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Proyecto #PonleCorrea. Pasear perros sin correa es un riesgo. Descarga el cartel y difunde el mensaje para salvar vidas.">
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/png" href="/images/favicon.png" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>

        <!-- La etiqueta <link> que teníamos aquí se ha ELIMINADO. -->
	</head>
	<body>
		<slot />

        <a href="#inicio" id="back-to-top-btn" aria-label="Volver arriba">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 19V5M5 12l7-7 7 7"/>
            </svg>
        </a>

        <script>
            const backToTopButton = document.querySelector("#back-to-top-btn");

            window.addEventListener("scroll", () => {
                if (!backToTopButton) return;
                if (window.scrollY > 400) {
                    backToTopButton.classList.add("visible");
                } else {
                    backToTopButton.classList.remove("visible");
                }
            });
        </script>
	</body>
</html>